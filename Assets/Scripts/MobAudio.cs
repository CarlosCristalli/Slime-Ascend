using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MobAudio : MonoBehaviour
{
    //NOT OPERATIONAL YET
    /*
    GameObject Player;
    public AudioSource MobSound;
    public AudioClip clip;
    private void Start()
    {
        Player = GameObject.FindGameObjectWithTag("Player");
        MobSound = GetComponent<AudioSource>();
    }

    private void FixedUpdate()
    {
        
        if(Vector2.Distance(transform.position,Player.transform.position) < 100)
        {
            
            MobSound.Play();
            Debug.Log(MobSound.isPlaying);
        }
        else
        {
            MobSound.Stop();
        }
    }
    */
   
}
